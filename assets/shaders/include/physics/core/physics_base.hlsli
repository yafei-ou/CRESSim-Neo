#ifndef CRESSIM_NEO_PHYSICS_CORE_BASE_HLSLI
#define CRESSIM_NEO_PHYSICS_CORE_BASE_HLSLI

#include "structured_buffer_compat.hlsli"

static const uint kInvalidIndex = 0xffffffffu;
static const float kEpsilon = 1.0e-6;

#endif // CRESSIM_NEO_PHYSICS_CORE_BASE_HLSLI
